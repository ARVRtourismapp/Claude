# Claude

This project is a browser-based Blackjack game.

## Testing the game

- Open `index.html` locally in a browser.
- Or run a local server with:
  - `cd /workspaces/Claude && python3 -m http.server 8000`
  - then open `http://localhost:8000`

## GitHub Pages link

After this branch is pushed and GitHub Actions completes, the game can be published at:

`https://arvrtourismapp.github.io/Claude/`
